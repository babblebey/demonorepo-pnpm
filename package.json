{
  "name": "demonorepo-pnpm",
  "version": "0.0.0",
  "private": true,
  "description": "A pnpm monorepo demo for testing monorepo-semantic-release",
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "lint": "pnpm -r lint"
  },
  "engines": {
    "node": ">=18",
    "pnpm": ">=8"
  }
}
